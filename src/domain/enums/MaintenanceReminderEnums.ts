export enum ReminderDueType {
  BY_DATE = 'BY_DATE',
  BY_ODOMETER = 'BY_ODOMETER',
  BOTH = 'BOTH',
}

export enum ReminderStatus {
  PENDING = 'PENDING',
  COMPLETED = 'COMPLETED',
  OVERDUE = 'OVERDUE',
}
