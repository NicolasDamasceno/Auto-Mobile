# data/database

Conexão com o SQLite (`expo-sqlite`), schema das tabelas (`vehicles`,
`expenses`, `maintenance_reminders`) e migrations. Implementa o storage
físico usado pelas classes em `src/data/repositories`.
